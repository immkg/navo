VITE_GOOGLE_MAPS_API_KEY=

# Leave unset — the app defaults to http://<the page's own hostname>:3001,
# which works both from localhost and from another device on the LAN.
# Only set this if the API is served from a different host than the page.
# VITE_API_BASE_URL=http://localhost:3001
